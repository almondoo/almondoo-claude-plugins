{
  "skill_name": "skill-eval",
  "evals": [
    {
      "id": 1,
      "name": "static-only-clean-skill",
      "prompt": "Please grade the skill at /Users/tm/development/oss/almondoo-claude-plugins/plugins/configure-github-permissions/skills/configure-github-permissions/ using static evaluation only. The dynamic layer is not needed. Produce a report.",
      "expected_output": "static.json + report.md. No hard_fail, around 0-2 warnings, expected score 0.7 or higher.",
      "assertions": [
        {"text": "static.json is generated", "kind": "factual"},
        {"text": "static.json follows the canonical schema (top-level keys include all of score / hard_fail / warnings / checks / target)", "kind": "format"},
        {"text": "static.json.score is reported as a number in 0.0-1.0 (not a custom scale like Grade A or out of 100)", "kind": "format"},
        {"text": "static.json.checks is an array, and each element has axis / passed / severity / evidence fields", "kind": "format"},
        {"text": "report.md includes a Static section and a verdict", "kind": "factual"},
        {"text": "The value of the hard_fail flag is reported", "kind": "factual"},
        {"text": "report.md notes that the dynamic layer was skipped", "kind": "factual"}
      ]
    },
    {
      "id": 2,
      "name": "ab-bench-real-skill",
      "prompt": "Run an A/B evaluation of the claude-md-parallel-audit skill (with vs without). One test prompt is enough: 'Audit this repository's CLAUDE.md'. One run per configuration is fine.",
      "expected_output": "benchmark.json + benchmark.md + report.md. Outputs for both with_skill and without_skill are saved under runs/, and pass_rate delta is computed.",
      "assertions": [
        {"text": "runs/eval-1/with_skill/ and runs/eval-1/without_skill/ both exist", "kind": "factual"},
        {"text": "benchmark.json includes run_summary.delta.pass_rate", "kind": "format"},
        {"text": "A differentiating_assertions section exists (even if empty)", "kind": "format"},
        {"text": "report.md verdict is one of: Ship-ready / Needs work / Net negative / Inconclusive", "kind": "format"}
      ]
    },
    {
      "id": 3,
      "name": "broken-skill-hard-fail",
      "prompt": "Create a skill directory at tmp/broken-skill/ (containing only a SKILL.md with no frontmatter) and evaluate it. It should detect hard_fail and skip the dynamic layer.",
      "expected_output": "static.json has hard_fail: true. The dynamic layer is not executed, and fix suggestions are written to report.md.",
      "assertions": [
        {"text": "hard_fail in static.json is true", "kind": "factual"},
        {"text": "report.md contains fix candidates (e.g., 'add frontmatter')", "kind": "factual"},
        {"text": "No subagent dispatch is performed (runs/ directory does not exist or is empty)", "kind": "behavioral"}
      ]
    }
  ]
}
